// UIDs of users who have access to feature-flagged tabs.
// Find your UID in Firebase Console → Authentication, or temporarily add
// console.log('UID:', userId) in App.jsx after the user is loaded.
export const FEATURE_FLAGS = {
  workTab: [
    'REPLACE_WITH_YOUR_FIREBASE_UID',
  ],
};
